@inject IConfiguration Configuration

@model IEnumerable<WebApplication1.Models.Blog>


@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Author)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PictureUri)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Author)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PictureUri)
            </td>
            <td>
                    @Html.ActionLink("Edit", "Edit", new { blogId = item.Id }) |
                    @Html.ActionLink("See Posts", "Index", "Posts", new { blogId = item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ }) |
                    <a href="/Blogs/CreateReport?blogId=@item.Id">Create Report</a> |

                   @* @if(item.Status == true)
                    {*@
                       

                    @{
                        var filename = item.Id + ".pdf";

                        var bucket = Configuration.GetSection("bucket").Value;

                        var resultingPath = bucket + "//" + filename;

                    }

                    <a href="https://storage.cloud.google.com/@resultingPath">Download</a>

                    @*}*@

            </td>
        </tr>
}
    </tbody>
</table>
